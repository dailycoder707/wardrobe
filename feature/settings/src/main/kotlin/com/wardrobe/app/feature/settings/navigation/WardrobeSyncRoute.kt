package com.wardrobe.app.feature.settings.navigation

import kotlinx.serialization.Serializable

/** Wardrobe Sync (Phase 8) — status, pending changes, storage used, manual
 * sync, sync history, backup export/restore. Reached from the same "no
 * Settings hub yet" entry point `WeatherSettingsRoute` uses. */
@Serializable
object WardrobeSyncRoute
