package com.wardrobe.app.feature.settings.navigation

import kotlinx.serialization.Serializable

/** The QR pairing flow (Phase 8) — "Show QR" (host role) and "Scan QR"
 * (scanning role) as two tabs of one screen/route, matching the brief's
 * single "Connect Phone" entry point. */
@Serializable
object PairingRoute
